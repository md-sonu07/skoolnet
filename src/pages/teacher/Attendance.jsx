import PagePlaceholder from '../../components/common/PagePlaceholder';

export default function Attendance() {
  return (
    <PagePlaceholder
      eyebrow="Teacher"
      title="Attendance"
      description="Teacher attendance page scaffold added for the requested folder structure."
    />
  );
}
