import PagePlaceholder from '../../components/common/PagePlaceholder';

export default function Dashboard() {
  return (
    <PagePlaceholder
      eyebrow="Teacher"
      title="Dashboard"
      description="Teacher dashboard scaffold added for the requested folder structure."
    />
  );
}
