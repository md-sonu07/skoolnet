import PagePlaceholder from '../../components/common/PagePlaceholder';

export default function Notes() {
  return (
    <PagePlaceholder
      eyebrow="Teacher"
      title="Notes"
      description="Teacher notes page scaffold added for the requested folder structure."
    />
  );
}
