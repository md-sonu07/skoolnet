import PagePlaceholder from '../../components/common/PagePlaceholder';

export default function Payments() {
  return (
    <PagePlaceholder
      eyebrow="Student"
      title="Payments"
      description="Student payments page scaffold added for the requested folder structure."
    />
  );
}
